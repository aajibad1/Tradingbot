# API Catalog (sandbox API plane)

Auto-generated by `scripts/build_api_catalog.py` from each service's live
OpenAPI schema (docs/06, docs/12). Per-service raw schemas are the
`*.openapi.json` files in this directory. SANDBOX — live rails gated on
licensing (docs/REGULATORY_BRIEF.md).

Services: 10 / 10

## public-api-gateway

- `PATCH  /api/{service}/{path}` — Proxy
- `GET    /api/{service}/{path}` — Proxy
- `PUT    /api/{service}/{path}` — Proxy
- `POST   /api/{service}/{path}` — Proxy
- `DELETE /api/{service}/{path}` — Proxy
- `GET    /healthz` — Healthz

## partner-auth

- `GET    /healthz` — Healthz
- `POST   /v1/partner/keys` — Issue Key
- `GET    /v1/partner/keys` — List Keys
- `POST   /v1/partner/keys/verify` — Verify Key
- `POST   /v1/partner/keys/{key_id}/revoke` — Revoke Key
- `POST   /v1/partner/keys/{key_id}/rotate` — Rotate Key

## routing-service

- `GET    /healthz` — Healthz
- `POST   /v1/routes/resolve` — Resolve
- `GET    /v1/routes/{route_id}` — Get Route

## onramp-orchestrator

- `GET    /healthz` — Healthz
- `POST   /v1/onramp/orders` — Create Order
- `GET    /v1/onramp/orders/{order_id}` — Get Order
- `POST   /v1/onramp/orders/{order_id}/advance` — Advance Order
- `POST   /v1/onramp/quotes` — Create Quote

## offramp-orchestrator

- `GET    /healthz` — Healthz
- `POST   /v1/offramp/orders` — Create Order
- `GET    /v1/offramp/orders/{order_id}` — Get Order
- `POST   /v1/offramp/orders/{order_id}/advance` — Advance Order
- `POST   /v1/offramp/quotes` — Create Quote

## wallet-service

- `GET    /healthz` — Healthz
- `GET    /v1/balances` — Balances
- `POST   /v1/wallets` — Create Wallet
- `GET    /v1/wallets` — List Wallets
- `GET    /v1/wallets/{wallet_id}` — Get Wallet
- `POST   /v1/wallets/{wallet_id}/adjust` — Adjust

## settlement-status

- `GET    /healthz` — Healthz
- `POST   /v1/events/ingest` — Ingest
- `GET    /v1/settlements` — List Settlements
- `GET    /v1/settlements/{order_id}` — Get Settlement

## webhook-service

- `GET    /healthz` — Healthz
- `GET    /v1/deliveries` — List Deliveries
- `POST   /v1/events/deliver` — Deliver
- `GET    /v1/webhooks/endpoints` — List Endpoints
- `POST   /v1/webhooks/endpoints` — Create Endpoint
- `POST   /v1/webhooks/endpoints/{endpoint_id}/replay` — Replay
- `POST   /v1/webhooks/endpoints/{endpoint_id}/rotate-secret` — Rotate Secret

## api-metering

- `GET    /healthz` — Healthz
- `GET    /v1/metering/check` — Check
- `POST   /v1/metering/quota` — Set Quota
- `GET    /v1/metering/quota` — Get Quota
- `POST   /v1/metering/record` — Record
- `GET    /v1/metering/usage` — Usage

## tenant-billing

- `GET    /healthz` — Healthz
- `POST   /v1/billing/invoices` — Create Invoice
- `GET    /v1/billing/invoices` — List Invoices
- `GET    /v1/billing/invoices/{invoice_id}` — Get Invoice
- `POST   /v1/billing/invoices/{invoice_id}/issue` — Issue Invoice
- `POST   /v1/billing/invoices/{invoice_id}/pay` — Pay Invoice
- `POST   /v1/billing/plans` — Create Plan
- `POST   /v1/billing/tenants/{tenant}/plan` — Assign Plan

_Total documented routes: 56_
