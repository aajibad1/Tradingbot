steps:
  - name: 'gcr.io/cloud-builders/docker'
    env: ['DOCKER_BUILDKIT=0']
    args: ['build', '-f', 'services/${_SVC}/Dockerfile', '-t', '${_IMG}', '.']
images: ['${_IMG}']
options:
  machineType: 'E2_HIGHCPU_8'
